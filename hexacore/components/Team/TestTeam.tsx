import { Text } from '@nextui-org/react';
import React from 'react';

const TestTeam = (props) => {

    return (
        <div>
            <Text>{props.text}</Text>
        </div>
    );

};
export {TestTeam}